//go:build ignore
// +build ignore

package main

// This file is intentionally ignored by the build system. The real driver
// implementations live in mididriver_native.go (build tag `native`) and
// mididriver_stub.go (default, non-native).

